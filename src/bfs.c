#include "bfs.h"
#include <stdio.h>

int bfs(Grid* g, int sr, int sc, int gr, int gc, Node* out_path, int* out_len) {

    // Mark unused parameters → hilangkan warnings
    (void)g;
    (void)sr;
    (void)sc;
    (void)gr;
    (void)gc;
    (void)out_path;
    (void)out_len;

    printf("[INFO] BFS belum diimplementasi. Placeholder dipanggil.\n");

    return 0;   // belum ada path
}
